package ch07.sec06.package2;

public class C {
    public void method() {
//        A a = new A(); // A 클래스의 생성자가 protected 이므로 접근 불가
//        a.field = "value"; // A 클래스의 필드가 protected 이므로 접근 불가
//        a.method(); // A 클래스의 메소드가 protected 이므로 접근 불가
    }
}
